<Icon Name="launch" class="@(Class.Merge(Color))"></Icon>

@code {
    [Parameter]
    public string Class { get; set; } = "";

    [Parameter]
    public string Color { get; set; } = "text-danger";
}